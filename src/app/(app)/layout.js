export const metadata = {
  title: 'Configuração',
  description: 'Configuração de perfil',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
